services:
  postgres:
    image: postgres:16
    container_name: sync_space_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: sync_space
      POSTGRES_USER: sync_user
      POSTGRES_PASSWORD: Janajeno_2004
      POSTGRES_HOST_AUTH_METHOD: trust  # <--- ADD THIS LINE
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: